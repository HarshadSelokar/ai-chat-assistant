{
  "name": "chat-interface",
  "version": "1.0.0",
  "description": "Chat interface with RAG using React, Node.js, Express, MongoDB, and Ollama",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["chat", "rag", "ollama", "mongodb", "react"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

